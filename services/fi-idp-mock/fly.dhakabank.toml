# Fly.io config for the Dhaka Bank instance of fi-idp-mock.
# One app per FI: deploy with `fly deploy -c fly.dhakabank.toml`.
# After the first deploy, set the public issuer (or rely on the
# FLY_APP_NAME fallback, which derives it automatically):
#   fly secrets set -a fi-idp-dhakabank ISSUER=https://fi-idp-dhakabank.fly.dev
app = 'fi-idp-dhakabank'
primary_region = 'sin'

[env]
  FI_ID = 'dhakabank'
  FI_NAME = 'Dhaka Bank'
  AUDIENCE = 'sbqr-fi-gateway'
  TOKEN_TTL_SECONDS = '300'
  REFRESH_TTL_SECONDS = '86400'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0

  [[http_service.checks]]
    grace_period = '10s'
    interval = '30s'
    method = 'GET'
    path = '/health'
    timeout = '2s'

[[vm]]
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256
