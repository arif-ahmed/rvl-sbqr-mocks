# hsm-mock — placeholder Dockerfile (scaffolding only).
#
# The runtime / base image for this service has not been chosen yet.
# This repository is a polyglot monorepo: each service may use a
# different language, framework, build system, or runtime.
#
# Replace the base image and build steps below when the service is
# implemented. Do not add package manifests (package.json, .csproj,
# requirements.txt, pom.xml, ...) until that happens.
FROM scratch
LABEL org.opencontainers.image.title="hsm-mock" \
      org.opencontainers.image.description="Mock HSM-related functionality for SBQR development and integration testing (scaffolding only — not implemented)."
